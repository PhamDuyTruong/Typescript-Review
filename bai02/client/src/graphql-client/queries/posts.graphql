query Posts($limit:Int!, $cursor: String){
    posts(limit: $limit, cursor: $cursor){
      totalCount
      cursor
      hasMore
      paginatedPost{
      id
    title
    text
    createdAt
    updatedAt
    textSnippet
    user{
      username

    }
      }
  }
}